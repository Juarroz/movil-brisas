<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

    <!-- Barra superior: top_app_bar + top_admin_bar -->
    <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appBarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#173622"
            android:elevation="8dp">

        <!-- Toolbar (siempre visible) -->
        <include layout="@layout/top_app_bar" />

        <!-- TabLayout para admin -->
        <include layout="@layout/top_admin_bar" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Contenido principal del formulario -->
    <include
            layout="@layout/content_contact_form"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>